N_278_project_site_norm_result_uuid_fix

База: N_277_project_site_norm_result_schema_fix.

Изменения:
- Исправлена ошибка загрузки результатов нормоконтроля:
  invalid input syntax for type uuid: "norm_result_...".
- При создании карточки результата нормоконтроля сайт больше не передает поле id.
- UUID карточки теперь создает сама БД через DEFAULT gen_random_uuid().

SQL:
- SQL выполнять не нужно.

Деплой:
1. Выбрать этот архив в автодеплое.
2. SQL не включать.
3. Выполнить обычный автодеплой сайта.
4. Проверить загрузку результата нормоконтроля повторно.

Vercel env:
- Не менялись.

GIP API:
- Не менялся. Требуется текущая версия с chunked upload.
