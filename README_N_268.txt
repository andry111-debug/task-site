N_268_project_site_norm_controller_sql_fix

Исправления:
1. Исправлена SQL-миграция роли нормоконтролера.
   Предыдущая миграция N_267 пересоздавала employees_role_check жестким списком ролей и могла падать, если в реальной базе уже были другие значения role.
2. Новая миграция N_268 сохраняет все существующие значения role в employees и добавляет norm_controller и связанные варианты.
3. В архиве оставлен только один SQL-файл, нужный для этой версии:
   supabase_sql/N_268_norm_controller_complete.sql
4. SQL N_268 также включает изменения N_266 для интерфейса нормоконтролера, поэтому старые N_266/N_267 SQL запускать не нужно.

Порядок применения:
1. В автодеплое выбрать этот архив.
2. Включить выполнение SQL.
3. Убедиться, что выбран только N_268_norm_controller_complete.sql.
4. Выполнить автодеплой.

Переменные Vercel не менялись.
GIP API не менялся.
Telegram webhook переустанавливать не нужно.
